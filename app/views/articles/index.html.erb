<h1 class="text-center mt-4 fw-semibold " >Listing all articles</h1>
<%# <%= will_paginate @article %>
<div class="flickr_pagination">
  <%= will_paginate @Articles, :container => false %>
</div>
<%= render 'article' %>
<div class="flickr_pagination mb-4">
  <%= will_paginate @Articles, :container => false %>
</div>
